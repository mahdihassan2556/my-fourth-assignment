Ans.1,
*getElementById:Grabs one specific element by its id.it is the fastest.

*getElementByClassName:Grabs all elements with a certain class as a list.

*querySelector:Grabs the first element that matches a css selector.

*querySelectorAll:Grabs all elements that match a css selector as a list.



Ans.2,
Creating and Inserting elements

*Create:Use

document.createElement('div') to make a new tag.

*Insert:Use appendChild() or pretend () to put it inside a parent element.




Ans.3,
It is a process where an event starts at the child element and moves up to its parents like a bubble.





Ans.4,
Instead of adding a listener to the parent. this is useful for handling items added later like saving memory.




Ans.5,
*preventDefault():stops the default action like a link opening or form refreshing.

*stopPropagation():stops the event from bubbling up to parent elements.